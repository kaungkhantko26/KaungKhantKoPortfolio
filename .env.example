# The static portfolio build needs no environment variables.

# Optional — only for `node scripts/sync-su-yet.mjs`, which refreshes the
# Su Yet Designs collection (/design/su-yet-designs) from Figma.
# Without these the committed fallback in src/data/suYet.ts is used.
FIGMA_TOKEN=
FIGMA_FILE_KEY=Rq9iC81fj5ho5T2vBNt9oV
